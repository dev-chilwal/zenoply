# Zenoply — Post-Launch Growth Roadmap

_Goal: grow organic search traffic. Last updated: 30 August 2026 (Phase 1 complete; Phase 2 complete — 85 tools live, 0 stubs remaining; Phase 3 advancing — 75 guides live; full SEO audit run 5 Jul — see below)._

The site is live at zenoply.com on Cloudflare Pages with git auto-deploy (`dev-chilwal/zenoply` → push to `main` rebuilds). The sitemap is verified and read successfully in Google Search Console (under the `zenoply.team@gmail.com` account). The growth model for a utility-tool site is simple: **every live tool and every guide is a separate page that can rank for its own search query.** More quality pages + clean on-page SEO + indexing = more traffic. This roadmap is ordered by impact-per-effort.

---

## Current state (updated 15 Jul 2026)

**85 tools now built and live in code, across 6 categories** (up from 9 at launch, 23 on 10 Jun, 43 on 25 Jun, 59 on 15 Jul). Each has an h1, meta description, FAQ block (feeds FAQ structured data), and is in the sitemap automatically. **75 guides are also live** under `/guides`. **No `coming soon` stubs remain — the catalog is fully live.** 9 live tools still lack a guide — that is the remaining Phase 3 backlog.

- **Text (8):** Word Counter, Case Converter, Remove Line Breaks, Remove Duplicate Lines, Find & Replace, Lorem Ipsum Generator, Slug Generator, Text Reverser
- **Dev (10):** JSON Formatter, Base64 Encoder/Decoder, URL Encoder/Decoder, UUID Generator, Hash Generator (MD5/SHA-1/256/512), JWT Decoder, SQL Formatter, HTML Minifier, QR Code Generator, LLM Token Counter
- **Convert (10):** Hex to RGB, RGB to Hex, Color Converter (HEX/RGB/HSL/CMYK), Unix Timestamp Converter, JSON to CSV, CSV to JSON, YAML to JSON, Number to Words, Rupees in Words, CGPA to Percentage
- **Finance (21):** SIP, Step-Up SIP, EMI, GST, FD, Compound Interest, Mortgage, Percentage, Simple Interest, RD, PPF, NPS, Gratuity, HRA, Income Tax, In-Hand Salary, Lumpsum, SWP, CAGR, ROI, Inflation
- **PDF (23) — category added since launch:** Merge, Split, Compress, Compress PDF to Size (KB), PDF to JPG, JPG to PDF, Rotate, Unlock, Protect (add password), Watermark, PDF Metadata Viewer & Remover, PDF to Word, PDF to Text, PDF to Excel, Resize PDF Pages, Crop PDF, Fill PDF Form, Extract Images from PDF, Add Page Numbers, Organize, Remove Pages, Remove Blank Pages, OCR PDF
- **Image (10):** Image Resizer, Image Converter (PNG/JPG/WebP), Image Compressor, Compress Image to Size (KB), Exam Photo & Signature Resizer, Passport Photo Maker, Image to Text (OCR), Crop Image, EXIF Viewer & Remover, HEIC to JPG

**No stubs remain.** The three previously deferred finance tools — Income Tax Calculator, In-Hand Salary Calculator, PPF Calculator — shipped 26 Jun (FY2025-26), alongside a Tier-1/Tier-2 finance expansion (Simple Interest, RD, NPS, Gratuity, HRA, Lumpsum, SWP, CAGR, ROI, Inflation) via PRs #4–#6. Tax-logic tools carry a "last updated / FY" note and need a yearly maintenance pass.

**Guides live (75):** Each is linked to/from its tool (bidirectional internal linking).
- _Text (4):_ Word Count, Character Count and Reading Time Explained · Text Case Formats Explained (UPPERCASE/Title/camel/snake) · What Is Lorem Ipsum? Meaning, History and When to Use It · What Is a URL Slug?
- _Finance (20):_ How Does SIP Investment Work? · How Is EMI Calculated? · How Is GST Calculated? · Mortgage Principal vs Interest, Explained · How to Calculate Percentage Increase · What Is Compound Interest? · How Is FD Interest Calculated? · How Is PPF Interest Calculated? · plus Old vs New Tax Regime, HRA, In-Hand Salary, Gratuity, NPS, RD, Lumpsum, SWP, CAGR, ROI, Simple Interest, Inflation
- _Dev (11):_ What Is Base64 Encoding? · What's Inside a JWT? · MD5 vs SHA-256: How Hashing Works · What Is JSON? · What Is a UUID? · What Is URL Encoding? · How to Format SQL Queries · Do QR Codes Expire? · What Is a Token in an LLM? · How to Format XML · How to Escape a String
- _Convert (9):_ What Is a Unix Timestamp? · How to Convert JSON to CSV · How to Convert CSV to JSON · RGB, HEX and HSL Color Codes Explained · How to Convert YAML to JSON · How to Write Numbers in Words · How to Convert CGPA to Percentage · How to Write Rupees in Words on a Cheque · How to Convert JSON to YAML
- _PDF (21):_ How to Merge PDF Files · How to Split a PDF · How to Compress a PDF · How to Convert JPG to PDF · How to Convert PDF to JPG · How to Convert PDF to Word · How to Extract Tables from a PDF to Excel · How to Unlock a PDF · How to Password Protect a PDF · How to Make a Scanned PDF Searchable (OCR) · How to Rotate a PDF and Save It · How to Add a Watermark to a PDF · How to Add Page Numbers to a PDF · How to Delete Pages from a PDF · How to Compress a PDF to 100KB (or Any Size Limit) · How to Remove Metadata from a PDF · How to Extract Text from a PDF · How to Change PDF Page Size (A4, Letter, Legal) · How to Crop a PDF (Trim Margins Without Losing Text) · How to Fill In a PDF Form and Flatten It · How to Extract Images from a PDF (Without Losing Quality)
- _Image (10):_ PNG vs JPG vs WebP · How to Make a Passport Photo at Home · How to Compress an Image · How to Compress an Image to a Specific Size in KB · How to Extract Text from an Image (OCR) · How to Resize an Image · How to Crop an Image · How to Remove EXIF Data from Photos · How to Resize a Photo and Signature for an Exam Form · How to Convert HEIC to JPG

Notable build notes:
- **Image tools** run entirely in-browser via the Canvas API — no uploads, no new npm dependencies, so the original 99 PageSpeed score is preserved. Passport Photo Maker has 36 country presets (grouped dropdown) at 300 DPI and **drag-to-position** (mouse + touch) plus zoom.
- **PDF tools** also run client-side (no uploads) but introduced the first heavy dependencies: `pdf-lib` (^1.17.1) and `pdfjs-dist` (^4.4.168). **Action: re-run a PageSpeed audit** to confirm these didn't dent the 99 score — they're lazy-loaded per-tool, but worth verifying since they're the first non-trivial bundles.
- **PDF to Word / PDF to Excel** (added 5 Jul) reuse `pdfjs-dist` for text extraction and add **zero new npm dependencies**: real `.docx`/`.xlsx` files are written by a tiny hand-rolled ZIP+OOXML builder (`components/tools/officeExport.js`, ~250 lines) instead of a heavy library like `docx` or SheetJS. Table detection (`components/tools/pdfTable.js`) reconstructs rows/columns from text coordinates via whitespace-band analysis. Both keep the light-bundle story intact.
- All formulas/crop math verified numerically; all components parse clean.

> **Deployment note:** changes are committed via `git push` to `dev-chilwal/zenoply`; Cloudflare auto-rebuilds. The production build can't run in the sandbox (the local dev server locks `.next`/`out`, and the file mount serves stale copies of recently-edited files) — verify via the Read tool + `npm run dev` locally, then push and let Cloudflare build. Confirm the live tool count after each push.

---

## SEO audit (5 Jul 2026) — verdict: technical SEO is done; the levers left are content, backlinks and time

A full codebase audit found **no critical technical SEO gaps**: all ~94 pages have unique titles/descriptions/canonicals, JSON-LD (SoftwareApplication, Article, BreadcrumbList, FAQPage) everywhere, verified sitemap, clean internal linking, 99 PageSpeed. Stop optimizing the tech. What actually moves rankings from here:

1. **Ride out the new-domain cold-start.** ~1 month into Google's evaluation period; expect 3–6+ months before meaningful rankings regardless of quality. The best signal during this window is a **steady publishing cadence** — don't pause the guide-per-day rhythm.
2. **Close the guide gap, GSC-ordered** (Phase 3). 9 tools still lack guides; write them in the order GSC impressions suggest, not alphabetically.
3. **Target long-tail question queries, not head terms** (Phase 3). "SIP calculator" is owned by Groww/ClearTax/ET Money — a new domain won't crack it soon. Question-shaped queries ("is gratuity taxable after 5 years", "PPF vs FD which is better", "how much SIP for 1 crore in 15 years") are winnable now and funnel to the calculators. Add comparison/scenario guides, not just formula explainers.
4. **Backlinks — the completely untouched lever** (Phase 5). A technically perfect site with zero referring domains stays in the sandbox longer. This is now the biggest gap.
5. **Weekly GSC feedback loop** (rhythm table). Impressions-without-guides → next guide; guides ranking positions 8–20 → expand/refresh (small gains there move to page 1). Bump the guide's `updated` date on real revisions — the sitemap reports true lastmod, so Google recrawls.
6. **Housekeeping:** GSC last showed 48 discovered pages; the site has ~94 now — re-check/resubmit the sitemap count.

Minor/optional from the audit: per-page OG images (social nicety, not a ranking factor); a table of contents on long guides (H2 ids already exist); Organization schema at the site root.

---

## Phase 1 — Confirm the foundation ✅ COMPLETE (9 Jun 2026)

All foundation checks passed. Results:

1. **Analytics — DONE.** Cloudflare Web Analytics enabled (zero-code, since the site is proxied through Cloudflare). Privacy-friendly, no cookie banner. Already showing first hits (mostly bots/crawlers at this stage — real users follow indexing; watch top pages + referrers, not raw country counts).
2. **Google Search Console — VERIFIED.** Property verified as a Domain property under `zenoply.team@gmail.com`. Sitemap (`https://www.zenoply.com/sitemap.xml`) status **Success**, **48 pages discovered** (the sitemap now covers more — re-check the discovered count). Page Indexing and Performance reports were still "processing data" on 9 Jun (normal for a property crawled the day before) with **no errors**.
   - **Multi-account gotcha:** Dev has several Google accounts signed in; zenoply.team is NOT the default. Use `/u/1/` style GSC URLs (e.g. `https://search.google.com/u/1/search-console/index?resource_id=sc-domain:zenoply.com`); plain URLs bounce to the wrong account.
3. **Bing Webmaster Tools — handoff.** Import from GSC at bing.com/webmasters (carries over verification + sitemap automatically). ~2 min, self-serve. (Bing's site is blocked from the browser automation tool, so Dev does this one manually.)
4. **Core Web Vitals — EXCELLENT.** PageSpeed mobile: **Performance 99, Accessibility 98, Best Practices 100, SEO 100.** FCP 1.5s, LCP 1.8s, CLS **0**, TBT **0ms**, Speed Index 2.5s. Brotli compression confirmed active (`content-encoding: br`). The "document request latency" insight was an unscored lab artifact — safe to ignore. _(Re-audit after the PDF deps — see build notes above.)_

**robots.txt & sitemap** also validated: both live and valid; sitemap referenced in robots.txt. Note: AI-training crawlers (GPTBot, ClaudeBot, Google-Extended, etc.) are blocked via Cloudflare's managed rules — this is fine and does NOT affect search indexing (`search=yes`).

### Performance backlog (de-prioritized — all optional, ~16 KiB total savings)
PageSpeed flagged these minor "insights" at score 99; none worth acting on now, recorded for completeness:
- Efficient cache lifetimes (~5 KiB) — static assets already immutably cached.
- Render-blocking CSS — single stylesheet; FCP already 1.5s.
- Legacy JavaScript (~11 KiB) — framework-controlled transpilation; trivial.

## Phase 2 — Ship tools on a steady cadence ✅ EASY/MID BATCH COMPLETE (ongoing for the long tail)

The launch catalog stubbed ~35 tools as `coming soon`. **All of them are now live** — the easy text/dev/convert wins, the full Image category, the whole PDF suite, and the complete finance calculator set including the three previously deferred tax tools. Catalog grew 9 → 23 → 43 → **56**, with 0 stubs remaining.

**✅ Shipped:** all of the text/dev/convert "next batch" (Find & Replace, Slug Generator, Text Reverser, RGB to Hex, Color Converter, JSON↔CSV, YAML to JSON, Number to Words, JWT Decoder, SQL Formatter, HTML Minifier), the **Image Tools** category (Resizer, Converter, Compressor, Passport Photo Maker), and the **PDF Tools** category (Merge, Split, Compress, PDF↔JPG, Rotate, Unlock, Watermark).

**✅ Stubs cleared (26 Jun):** Income Tax Calculator, In-Hand Salary Calculator and PPF Calculator shipped for FY2025-26, plus a Tier-1/Tier-2 finance expansion (Simple Interest, RD, NPS, Gratuity, HRA, Lumpsum, SWP, CAGR, ROI, Inflation). **Maintenance note:** the tax/salary tools need a yearly review when slabs change — add to the quarterly cadence below.

**✅ Suite-wide upgrade (10 Aug): PDF password gate.** Every PDF tool now accepts password-protected files: `PdfDropzone` runs each dropped PDF through a shared gate (`components/tools/pdfCrypto.js`) — owner-only restrictions are stripped silently, open-password files get an inline password prompt, and tools receive ordinary decrypted bytes with zero changes to their processing code. Engine is qpdf 12.2.0 wasm (`@neslinesli93/qpdf-wasm`, pinned; 1.3 MB binary self-hosted at `public/qpdf.wasm` via `scripts/copy-qpdf-assets.mjs`, lazy-loaded only when a dropped file actually carries encryption). Chosen over `@cantoo/pdf-lib` after parallel research: qpdf decrypts structure-preservingly (tools keep seeing the input shapes they were tested on), handles AES-256 R6 both ways, and its encrypt side means **Protect PDF (add password) is now S effort, not M**. Unlock PDF was retargeted as the "remove PDF password" page (title/desc/FAQs + guide updated; rotate/watermark/page-numbers/remove-pages guide troubleshooting lines updated to match). Verified end-to-end on the static build: AES-256 + AES-128 prompts, wrong-password error, owner-only silent strip, plain-file passthrough, decrypted output feeding both pdf-lib (Rotate: /Rotate 90 on all pages) and pdf.js (PDF to JPG: 3 images) pipelines, and mixed plain+encrypted Merge (6-page output, one prompt, order preserved).

**Future tool ideas (net-new, beyond the original stub list):**
- Image: background remover (the old `E:\utilio` app used `@imgly/background-removal` + onnxruntime — heavy; only add if worth the bundle cost) and image cropper.
- More converters/dev tools by GSC demand — let Search Console queries pick the next batch rather than guessing.

> With the easy backlog cleared, the growth engine shifts from "ship any tool" to **"ship what data says people search for"** (Phase 3 guides + GSC-driven picks).

## Phase 3 — Content / guides for long-tail and authority 🔄 UNDERWAY

Tools win the "[thing] calculator" query; **guides win the "how/what/why" queries** and build topical authority that lifts the whole domain. The `/guides` section is live and pairs each guide with its tool (internal linking both ways — the cheap SEO multiplier most sites skip). Each guide is 850–1,200 words with a worked example where relevant, 3 FAQs (FAQPage JSON-LD), Article schema, and ≥2 links to its tool. Every guide is paired with a `guide:` backlink on the tool entry in `lib/site.js`, so the link is bidirectional.

**✅ Live (26):**
- _Finance:_ SIP → SIP Calculator · EMI → EMI Calculator · GST → GST Calculator · Mortgage Principal vs Interest → Mortgage Calculator · Percentage Increase → Percentage Calculator · What Is Compound Interest? → Compound Interest Calculator · How Is FD Interest Calculated? → FD Calculator · How Is PPF Interest Calculated? → PPF Calculator · Old vs New Tax Regime → Income Tax Calculator · How Is HRA Exemption Calculated? → HRA Calculator · How Is In-Hand Salary Calculated From CTC? → In-Hand Salary Calculator
- _Dev:_ What Is Base64 Encoding? → Base64 Encoder · What's Inside a JWT? → JWT Decoder · MD5 vs SHA-256 → Hash Generator · What Is JSON? → JSON Formatter · What Is a UUID? → UUID Generator · What Is URL Encoding? → URL Encoder · Do QR Codes Expire? → QR Code Generator · What Is a Token in an LLM? → Token Counter
- _Convert:_ What Is a Unix Timestamp? → Unix Timestamp Converter
- _PDF:_ How to Merge PDF Files → Merge PDF · How to Split a PDF → Split PDF · How to Compress a PDF → Compress PDF · How to Convert JPG to PDF → JPG to PDF · How to Convert PDF to JPG → PDF to JPG
- _Image:_ PNG vs JPG vs WebP → Image Converter · How to Make a Passport Photo at Home → Passport Photo Maker · How to Crop an Image → Crop Image

> All 20 guides were drafted and fact-checked via a multi-agent workflow (draft → accuracy + SEO/style verification → revise), then integrated deterministically into `lib/guides.js` + `lib/site.js`. Formulas and worked examples (compound interest 2,15,892; FD 1,41,478) were recomputed and tool UX was checked against the actual components (e.g. JPG-to-PDF reorders via Up/Down buttons, not drag). `npm run build` generated all 20 guide pages cleanly (76 static pages total). Batch 1 (10 guides) and batch 2 (5 guides) shipped 25 Jun.

**Next guides to write (remaining live tools that lack a guide — note each tool can show only one `guide:` backlink, so prioritise tools with none):**
- ✅ "RGB, HEX and HSL Color Codes Explained" → Color Converter — shipped 15 Jul (all four systems: HEX as three base-16 channel pairs + 3-digit shorthand doubling + RGB additive 0–255 + HSL hue/saturation/lightness for intuitive editing + CMYK subtractive ink with the black 'key' plate and device-dependence caveat + worked example carrying #3b82f6 → rgb(59, 130, 246) → hsl(217, 91%, 60%) → cmyk(76%, 47%, 0%, 4%) — verified against the tool's own math + which-format-when + links to Hex to RGB / RGB to Hex).
- ✅ "How to Extract Text from an Image (OCR)" → Image to Text — shipped 20 Jul (what OCR does stage by stage: binarise/deskew → layout analysis → line-level neural recognition vs old glyph-template matching + why the language setting is the biggest accuracy lever and when eng+hin actually helps + what makes OCR accurate, resolution ~300 DPI / contrast / straightness / lighting / light compression, plus the counterintuitive "don't compress before OCR" + worked example on a phone-shot invoice: >95% chars correct, predictable 0/O 1/l 5/S 8/B swaps to verify by eye, table structure not preserved + where OCR struggles + images vs scanned PDFs pointing to OCR PDF and the select-a-line check that sends you to PDF to Word instead + in-browser privacy for payslips/IDs + cleanup via Remove Line Breaks / Find & Replace / Word Counter).
- ✅ "How to Resize an Image" → Image Resizer — shipped 24 Jul (**every image tool now has a guide**). Covers what resizing changes (pixel grid rebuilt, neighbouring pixels averaged on the way down, high-quality smoothing) vs cropping vs compressing, and the resize-then-compress order for web + aspect ratio and the default 'Lock aspect ratio' behaviour (type width → height derived from the original proportions) + worked example on a 12 MP 4032×3024 phone photo → 1200 wide snapping to 900 (4032 ÷ 3024 = 1.3333), 3.36× narrower linearly but 12,192,768 → 1,080,000 pixels, under 9% of the original / ~91% fewer, since pixel count scales with the square + the preset trap (all four presets switch the ratio lock OFF, so a 4:3 photo forced into 1080×1080 is squeezed horizontally by 25% because it wants to be 1440 wide) + why upscaling never adds detail (400 → 1600 = 16× the pixels, no extra information) + target dimensions per use case incl. the print DPI distinction (4×6 in at 300 DPI = 1200×1800) + PNG-only output pointing to Image Compressor / Image Converter.
- ✅ "What Is a URL Slug? How to Create One (and Why It Matters for SEO)" → Slug Generator — shipped 25 Jul (what a slug is + URL anatomy breakdown protocol/domain/path/slug + the newsroom origin of the term + why slugs matter for readability, as a keyword ranking signal and for shareable/stable links + the good-slug rules: lowercase, hyphens, keywords-not-filler, short, plain ASCII, stable + how a title becomes a slug via the tool's exact pipeline — NFKD accent-strip → lowercase → collapse every non-alphanumeric run to one separator → trim dangling separators + worked example "Café René: A Beginner's Guide (2024 Edition)" → cafe-rene-a-beginners-guide-2024-edition, verified against the tool's own slugify + hyphens vs underscores: Google reads a hyphen as a word separator and an underscore as a joiner, so best-pizza-recipe = three words vs best_pizza_recipe = one token, pointing underscore/snake_case uses to Case Converter + should-you-change-a-slug: avoid it, 301-redirect if you must, get it right before publishing).
- ✅ "How to Crop an Image" → Crop Image — shipped 3 Aug alongside the tool itself (what cropping changes and what it leaves alone: a sub-rectangle of the pixel grid copied at original resolution with nothing resampled, so it is one of the few edits that cannot soften a photo — the opposite of resizing, which rebuilds the grid and averages + file size follows area, a 4032×3024 phone photo down to its largest square keeps exactly 75% of the pixels because you keep 3024 of 4032 columns and every row + aspect-ratio locking with each preset's fit verified against the tool's own arithmetic: on 4032×3024, 1:1 → 3024×3024 at X 504, 16:9 → 4032×2268 at Y 378, and 4:3 selects the whole image because the photo is already 4:3 + cropping to exact pixels via the X/Y/W/H fields, and the clamping that explains the behaviour that looks like a bug — a 320-wide box on a 400-wide image will not let X past 80 + worked example taking a landscape phone photo to a square avatar, then crop-then-resize order because that picks the framing before any pixels are averaged away + crop vs resize vs compress and where each belongs, pointing at Image Resizer / Image Compressor / Passport Photo Maker / Image Converter).
- ✅ "Do QR Codes Expire? Static vs Dynamic QR Codes Explained" → QR Code Generator — shipped 5 Aug alongside the tool itself (the pattern never expires — a QR code is a frozen grid with no timestamp, serial or callback — but a **dynamic** code encodes a short link owned by the generator company, so the redirect in the middle is the part that lapses when a trial ends or the provider closes + the two kinds are visually indistinguishable because size tracks byte count, not type: `https://zenoply.com` at 19 bytes and a typical `https://qr.example/aB3xK9` short link at 25 bytes both land on the same 25×25 version-2 grid at ECC M, verified against uqr + the five-second test, read the URL your camera previews, plus the tool's own "Encoded data" readout showing the exact string before you download + the four real causes of dead codes: lapsed redirect service, moved destination, printing too small/low-contrast, physical damage + error-correction trade-off with the density cost made concrete — the 41-byte tool URL needs 29×29 at M but 37×37 at H, so cranking to H on a small print can *cost* scans + the four-module quiet zone the standard requires and the tool warns below + print sizing rule of thumb, width ≥ 1/10 of scan distance, and the sub-0.5mm module trap + the honest case for dynamic: unknown destination or scan analytics, but point a static code at a path on your own domain and a self-owned redirect gives the same flexibility with nothing to renew).
- ✅ "How to Convert CGPA to Percentage (CBSE 9.5, 10-Point and VTU Rules)" → CGPA to Percentage Calculator — shipped 6 Aug alongside the tool itself (there is no single national conversion, only a handful of institution-specific published rules plus folklore, so the guide leads with the rules that are actually written down + what a CGPA measures and the credit-weighted formula `sum(grade point × credits) / sum(credits)`, the weighting being the part people skip — a 4-credit paper pulls twice as hard as a 2-credit lab, so a plain average of grade points will not match what the university prints, and SGPA/CGPA/semester-CGPA are all the same arithmetic at different scopes + worked example verified numerically: 127 weighted points over 15 credits = CGPA 8.4667, which the three rules scatter across 80.43% (CBSE ×9.5), 84.67% (×10) and 77.17% (VTU, 0.75 offset first) — a seven-point spread off one grade card, which is the whole reason "what is my percentage" has no answer until you know whose rule applies + where the 9.5 actually comes from: CBSE's own A1 band (91–100) averages ~95 marks against a grade point of 10, so 95 ÷ 10 = 9.5, which is also why a perfect 10.0 caps at 95% and why the same multiplier works per-subject + why universities disagree, incl. banded-table schemes no multiplier reproduces and equivalence certificates that outrank any formula + reversing each rule, with the caveat that a reverse conversion is only valid under the rule that produced the percentage, and that CBSE's 95% cap makes anything above it yield an impossible CGPA over 10 + the double-rounding trap that explains most "my calculator says something else" reports: round 8.46667 to 8.47 first and ×9.5 gives 80.47%, carry full precision and it gives 80.43% — round once, at the end, which is what the tool does + why a self-computed 4.0 GPA does not belong on an application, since WES and admissions offices use non-linear grade-band tables + authority order for what to write on the form, and stating the rule alongside the number so a discrepancy reads as two conventions rather than a misstated mark).
- ✅ "How to Write Rupees in Words on a Cheque (India)" → Rupees in Words — shipped 7 Aug alongside the tool itself (deliberately India-specific so it does not cannibalise the general "How to Write Numbers in Words" guide, which stays short-scale and country-neutral + the two amounts every cheque carries and their clearing-system names, the courtesy amount in the figures box and the legal amount on the printed "Rupees" line, with Section 18 of the Negotiable Instruments Act, 1881 making the words govern — paired with the practical reality that a mismatch is returned rather than paid, and that CTS rules bar correcting it on the spot, so a bad cheque is replaced not amended + why the form fixes the word order for you: "Rupees" is pre-printed, so the currency is never repeated at the end + five-step worked example on 1,23,456.50 → "One Lakh Twenty-Three Thousand Four Hundred Fifty-Six and Fifty Paise Only", verified against the tool's own output + the place-value trap that dominates this topic: .50 is fifty paise and .05 is five, a factor-of-ten error that survives proofreading, plus rounding to the nearest paisa before writing anything + lakh/crore grouping against the short scale, incl. the point people convert wrongly by eye — above a crore the Indian system reuses its own words rather than minting new ones, so 10^9 is one hundred crore and 10^12 is one lakh crore, with no "billion" equivalent arriving + the four things that actually get cheques returned (words vs figures, any alteration under CTS, space left after the words, and the three-month staleness window) + hyphens 21–99, the optional "and", capitals and the word Only sorted into conventions vs requirements so the reader stops worrying about the wrong ones).
- ✅ "What Is a Token in an LLM? How GPT and Claude Count Text" → Token Counter — shipped 9 Aug alongside the tool itself (every figure measured against the shipped tokenizer rather than recalled, because this topic is thick with confidently-wrong round numbers + what a token is: text is chopped into vocabulary fragments and swapped for integers before the model sees anything, and because byte-pair merges follow frequency, common sequences become single entries and rare ones stay in pieces — the one property behind every surprise in a count + why the count never matches a word count, made concrete: " the" is one token including its leading space, " antidisestablishmentarianism" is six as " ant"+"idis"+"est"+"ablishment"+"arian"+"ism", and " Zenoply" costs three, so splitting on spaces gives the wrong answer + the encoding matters as much as the text: the tool's own sample is 84 tokens under o200k_base and 88 under cl100k_base, which is also the answer to "why does another site disagree with you" + the non-English section is the strongest evidence in the guide — one instruction sentence costs 11 tokens in English, 23 in Hindi, 13 in Arabic and 19 in Japanese at ~1.3 chars/token, and that same Hindi sentence costs 69 under cl100k_base against 23 under o200k_base, a three-fold swing that makes the encoding matter far more outside English than inside it + the four tiktoken encodings mapped to model families, with the reasoning for keying the tool on the encoding rather than the model name: line-ups get renamed every few months, encodings do not + the honesty section on Claude and Gemini — neither vendor publishes a browser-runnable tokenizer, so any exact offline count is a guess; the guide names the characters÷4 basis, its 10–20% English error band, its much worse behaviour on code and non-Latin scripts, and points at each vendor's own counting endpoint + context windows have to hold the reply too, which is the mistake behind most truncated outputs + cost worked through to the point where it stops being trivial: 4,000 words ≈ 4,400 tokens ≈ $0.013 per send at $3/1M, but ~$1.33/day at 100 calls and ~$485/year for one uncached document).
- ✅ "How to Compress an Image to a Specific Size in KB" → Compress Image to Size (KB) — shipped 10 Aug alongside the tool itself (targets the compress-to-20KB/50KB/100KB queries that exam and job-application forms generate, a demand pattern the existing general "How to Compress an Image" guide deliberately does not chase + leads with the unit ambiguity that causes the most baffling rejections: a KB is 1,024 bytes so a 50 KB cap is 51,200, but a minority of portals compute 50,000 — 1,200 bytes/2.4% tighter, exactly the margin a tool aiming just under 51,200 occupies, so a file that looks comfortably inside gets refused + the two dials that actually move file size, quality as 8x8-block quantisation vs pixel count as the multiplier that quarters the file when both dimensions halve, and why PNG has neither in useful form — lossless means no quality dial, which is why every target-size tool writes JPG or WebP + worked example converting the target into a per-pixel bit budget, which makes the whole problem obvious: 50 KB is 409,600 bits, a comfortable 1.33 bits/pixel at 640x480 against 24-bit uncompressed colour — an unremarkable 18:1 ask — but only 0.034 bits/pixel at 4000x3000, a 700:1 ask the encoder will still satisfy and which will still look terrible, which is the arithmetic argument for resizing rather than grinding quality down + the caps forms actually set, hedged as ranges rather than stated as rules since only the page in front of you counts: 20–50 KB photo, 10–20 KB signature, 100–500 KB documents + the three fixes when the output is legible but ugly, in order of payoff — crop first so no byte is spent on background, resize deliberately to the form's stated dimensions, then WebP if accepted + the rejection FAQ covering the cases a byte target alone cannot fix: decimal-kilobyte portals, enforced pixel dimensions or aspect ratios, format allowlists that refuse WebP at any size, and minimum-size rules + in-browser privacy, which carries real weight here because the images with byte limits attached are passport photos, scanned signatures and identity documents).
- ✅ "How to Compress a PDF to 100KB (or Any Size Limit)" → Compress PDF to Size (KB) — shipped 11 Aug alongside the tool itself (the PDF half of the compress-to-target pair, targeting the compress-pdf-to-100kb/200kb/500kb queries; leads with the same unit ambiguity as its image sibling — 100 KB is 102,400 bytes, but a minority of portals compute 100,000, about 2.4% tighter and exactly the margin a tool landing just under 102,400 occupies + the part that is specific to PDFs and that no competitor explains: a PDF is a container that spends bytes on itself before a single pixel is drawn, measured from real output at roughly 330 bytes per page plus ~600 fixed, which is why a 10-page document cannot reach 2 KB and why the page count, not the quality slider, sets the real floor + worked example converting a 200 KB / 3-page budget into bits per pixel, showing the same budget go from 0.42 bits/pixel at 115 DPI to about 1.0 at 75 DPI purely by drawing fewer pixels, and JPEG holding up at ~1 bit/pixel — arithmetic that predicts the shipped tool's actual choice of ~75 DPI and 198.7 KB on that file + states plainly what the approach costs, that text stops being selectable, and that a plain text PDF is a poor candidate that can grow rather than shrink + the escape routes when a target is out of reach, in order of payoff: send fewer pages via Remove Pages or Split PDF, check whether the form actually wants a JPG rather than a PDF, or raise the target + in-browser privacy, which carries weight because the documents with byte limits attached are certificates, identity documents and bank statements)
- ✅ "How to Resize a Photo and Signature for an Exam Form" → Exam Photo & Signature Resizer — shipped 14 Aug alongside the tool itself (targets the SSC/IBPS/NTA exam-form photo-and-signature queries, the highest-winnability niche in the Aug research run, and deliberately does not overlap the compress-to-size guides because the distinguishing rule here is a **minimum** file size, which ordinary compressors ignore entirely + separates the three things a portal checks independently — format, dimensions in px or cm, and a KB *range* — since a rejection names none of them + the analytical core is bits-per-pixel arithmetic showing which end of the range binds and why it flips between exams: a 200×230 bank photograph is 46,000 pixels, so 20–50 KB asks for 3.6–8.9 bits/pixel against 24 raw, a 2.7:1 reduction JPEG beats without trying, which is why such photos land *under* the floor — measured at 14,055 B at quality 1.0 on a soft source — while SSC's 413×531 at the same stated range buys only 1.87 bits/pixel and binds on the ceiling instead, landing at 49,579 B after a 7-pass search; same range, opposite failure, purely from 4.8× the pixel count + the honest remedy for an under-minimum file: enlarge the pixel dimensions, since portals publish dimensions as "preferred" while enforcing the KB range as a hard check, verified end-to-end at 241×278 / 23,565 B in band + states plainly that filler-byte padding is not used, which every competitor in this niche does silently + signature-specific handling: fit-whole-with-white-padding so nothing is cropped, black ink on white paper under flat light, and the background-cleanup side effect that a flattened white field compresses smaller and so needs enlarging more often + the 1,024-vs-1,000 byte ambiguity applied to *both* ends of a range + closes on why an exam photo and specimen signature — most of what is needed to impersonate someone on paper — should never leave the device)
- ✅ "How to Convert HEIC to JPG" → HEIC to JPG — shipped 15 Aug alongside the tool itself (inherits the "heic to jpg" tail, the top autocomplete suggestion for a bare "heic" and a SERP of small converter sites, with the free/no-upload angle doing the work + explains the format rather than just the procedure: HEIF container, HEVC/H.265 codec, iPhone default since iOS 11 in 2017, an MPEG standard rather than an Apple invention, supported on Android since 10 + the support split that actually explains the reader's problem — Apple opens them everywhere, Windows 11 ships the container but not the decoder and sells the codec separately, browsers have no HEIC decoder at all so the file will not even render in an img tag, and "JPG or PNG only" forms mean it + the size surprise answered with measured numbers rather than hand-waving, on a real 3840x2160 photograph decoded once and re-encoded by each codec: 23.7 MB raw, 783 KB as HEIC at 0.77 bits/pixel, 1.77 MB as JPEG q90 at 1.79 bits/pixel and 2.3x the HEIC, 1.37 MB at q80 still 1.8x, and 9.83 MB as PNG at 12.9x — so "my JPG got bigger" is the efficiency being handed back, not a bug, and the two honest levers are the quality slider or Compress Image to Size + the rotation section, the real differentiator against the incumbent converters: HEIC records rotation as a container flag rather than in the pixels, ignoring it is what produces the sideways photos free converters are known for, and the shipped decoder applies it — verified by patching a fixture to irot=1 and getting 48x64 back from a 64x48 file — with the displayed dimensions serving as the reader's own check + format guidance that says JPG unless there is a reason, PNG only for screenshots and line art, WebP for the web but not for a form + the canvas re-encode dropping all EXIF, framed as the privacy win it is and cross-linked to the EXIF tool, with the one thing worth noting first: the capture date + FAQs on the three real queries — converting free on Windows, whether quality is lost, and turning HEIC off on the iPhone, including the Settings > Photos > Transfer to Mac or PC > Automatic middle path that keeps the storage saving on the device while handing JPGs to everything else)
- ✅ "How to Extract Text from a PDF" → PDF to Text — shipped 20 Aug alongside the tool itself (targets the "extract text from pdf" / "pdf to text" tail without overlapping the PDF-to-Word guide, because the question behind it is different: not "how do I get an editable document" but "why does the text I copied come out wrong" — so the guide leads with the cause rather than the procedure. A PDF has no lines and no paragraphs; it stores glyphs at coordinates the way it stores a rectangle, and everything a reader perceives as structure is reconstructed by the eye. Three consequences follow and they account for nearly every bad paste: a paragraph that wrapped over five lines is five unrelated runs of text at five heights, so pasting yields five hard breaks that re-wrap badly the moment the font or width changes; spaces are frequently absent because many writers position the next word rather than emit a space character, which is what produces "InvoiceTotal" from a page that plainly reads "Invoice Total"; and draw order is not reading order, since instructions arrive in whatever sequence the producing software emitted them — rarely the human order for a two-column layout, a table or a form + the flowing-paragraphs vs keep-line-breaks choice carried by a worked example that makes the trade-off concrete: an unpaid-invoice sentence wrapped over two lines followed by a three-line address, where the first pair must be rejoined and the last three must not, and the discriminator is measured rather than fixed — the median vertical gap for that document, since documents vary in leading and a threshold in points would break on the next file + page ranges validated against the real page count as you type, so a typo is caught before anything runs + the scan case answered with a test the reader can run in a second, i.e. try selecting a word: no highlight means no text layer and no extractor of any kind can help, which routes to OCR PDF rather than leaving a dead end + what plain text cannot carry, routing tables to PDF to Excel and document shape to PDF to Word instead of pretending .txt covers them, plus the two sharp edges worth stating — text baked into an embedded image extracts nothing even in an otherwise selectable document, and the .txt carries a UTF-8 BOM so Notepad on Windows renders accents and non-Latin scripts rather than mojibake + FAQs on the three real queries: the line-break-per-line complaint, password-protected files, and whether two-column pages come out in the right order — answered honestly, since side-by-side columns can share a baseline and be read as one line, so papers and newsletters are worth checking and often extract better one column at a time)
- ✅ "How to Change PDF Page Size (A4, Letter, Legal)" → Resize PDF Pages — shipped 21 Aug alongside the tool itself (targets the "change pdf page size" / "pdf a4 to letter" / "resize pdf to a4" tail, a query family the incumbents answer badly because their tools flatten the document. Leads with the mechanism rather than the click path: page size is the **MediaBox** rectangle, what you see is the **CropBox**, and neither has anything to do with resolution — text and vectors are drawing instructions, so they are re-drawn at the new size rather than resampled and stay sharp at any scale, with embedded photographs the only exception and only on a large enlargement. Letter → A4 is worked numerically because the arithmetic *is* the answer to the commonest complaint: the fit takes the smaller of 595.28/612 = 0.9727 and 841.89/792 = 1.0630, so the content fills the A4 width exactly and the 72 points left over split into ~12.7 mm above and below — which is the honest reply to "why is there a white border on my converted PDF". A4 → Letter binds the other way at 0.9407 and leaves ~9 mm down each side; removing either band would mean cropping or distorting, so neither is offered. Then fit vs actual-size vs scale-by-percent with the case for each, mixed page sizes and the keep-each-page's orientation default that stops a landscape chart being turned on its side, and rotation — a PDF can store a page one way round and display it another, so the tool works from what you see and preserves the flag rather than baking it in. A whole section separates **resizing from compressing**, because "resize" means something different for images and the two are conflated constantly: changing page size changes the sheet, not the data, so a 4 MB Letter PDF is a 4 MB A4 PDF — measured at <1% movement on the real conversion — which routes the actual intent to Compress PDF and Compress PDF to Size instead of disappointing the reader. Closes on what survives, the genuine differentiator: links, bookmarks, form fields and comments come across and text stays selectable, because pages are resized in place rather than redrawn as pictures. FAQs on the three real queries: does it lose quality, why the white border, and can I resize only some pages)
- ✅ "How to Crop a PDF (Trim Margins Without Losing Text)" → Crop PDF — shipped 22 Aug alongside the tool itself (targets the "crop pdf" / "trim pdf margins" / "remove white space from pdf" tail. Leads with the mechanism, because it explains every property of the result at once: a crop moves two rectangles — the **MediaBox** sheet and the **CropBox** a reader displays — and touches no drawing instruction, which is simultaneously why text stays vector-sharp, why it is instant on a 300-page file, why the file size barely moves, and why the cropped-away content is still there. Notes that both boxes are written, not just the CropBox, since print pipelines lay out from the MediaBox and would ignore a CropBox-only trim on exactly the machine that mattered. Auto-trim is explained by what it has to survive rather than what it does: one speck of scanner dust would hold the whole margin open, so a mark counts only once connected to something — hairlines kept, specks dropped — and blank pages are skipped rather than read as full-bleed, with the sampling on long documents stated rather than hidden. Worked example checked numerically: an A4 text block inset 60/60/70/80pt with 5pt of padding gives 19.4 / 22.9 / 26.5 mm trims, a 485.28 x 701.89pt (171 x 248 mm) page and 68% of the sheet kept. Rotation gets its own note, since on a /Rotate 90 page the edge you see at the top is the edge stored on the left, and mixed-size documents are covered by the crop being held as a proportion rather than a fixed millimetre trim that a small page could not satisfy. The section that earns its place is **"A crop is not a redaction"** — the danger is not that people do not know cropping hides content, it is that it *looks like it worked*: pdf.js-based extractors drop text outside the page box, so a crop passes a text-extraction check and still hands everything to the next tool. Both halves were verified against real pdf.js rather than asserted. Closes on what cropping will not do — it is not compression and it does not give a standard paper size — routing those to Compress PDF / Compress PDF to Size and Resize PDF Pages, and on what survives, since pages are cropped in place. FAQs on the three real queries: can the cropped part be recovered, does it shrink the file, and does it blur anything)
- ✅ "How to Fill In a PDF Form and Flatten It" → Fill PDF Form — shipped 23 Aug alongside the tool itself (targets the "fill pdf form" / "flatten pdf" / "make pdf not editable" tail, and opens on the two failures that actually bring people to the query: you cannot type into the file at all, or you filled it in and were told to resend it "not editable". Both are the same machinery, so the guide leads with what an AcroForm is and why most form-looking PDFs have none — the boxes are printed artwork in the same drawing instructions as the headings — with XFA named as the third case, since those files show Acrobat’s "Please wait..." placeholder everywhere else and the tool detects them rather than showing an empty list. Flattening is explained by a test the reader can run rather than an assertion: extract the text from a filled-but-unflattened file and the answers are **absent**, because they live in the field objects rather than the page — which is also why a printer or a different viewer can disagree about what the document says; flatten and the same extraction returns them. Verified in both directions against real pdf.js. States plainly what flattening is not — it locks the text, not the file — and routes that to Protect PDF. Signature fields get their own section, because the honest answer is that no in-browser tool can produce a valid signature, and drawing a picture of a name in that box would hand the reader something that looks signed and is not; flattening removes the empty box rather than faking it, and signing has to come after flattening or the fill breaks the signature. The section with the most India-specific value is the font one: appearances are redrawn in WinAnsi Helvetica, which covers José and café and the euro but **not the rupee sign, Devanagari, CJK or emoji** — so those values are saved and drawn by the reader but cannot be flattened, since flattening would bake in an appearance that does not exist and lose the answer outright. FAQs on the three real queries: can flattening be undone, why Acrobat can type on a PDF this tool reports as fieldless, and whether flattening fixes a form that prints blank)
- ✅ "How to Convert JSON to YAML" → JSON to YAML — shipped 26 Aug 2026 alongside the tool itself (targets the "json to yaml" / "convert json to yaml online" tail, a query the incumbents answer with a bare textarea. The guide leads with the two failure modes that actually cost people something, because both are invisible until production: YAML types unquoted scalars by *shape*, so a string like the country code `NO` comes back as the boolean `false` under the YAML 1.1 rules PyYAML and Ruby's Psych still use — the "Norway problem" — and the same rules re-type `017` as octal, `1_000` as a thousand, `1:30` as ninety seconds, `2026-08-25` as a date and a version `1.20` as `1.2`; and a converter built the obvious way on `JSON.parse` silently rounds a 20-digit ID to `...567000` and rewrites `1.50` as `1.5`. Also covers the block-scalar form for multi-line text with its `|-` chomping indicator, the `{}`/`[]` borrowings for empty collections, duplicate-key last-wins, and the 2-vs-4-space indent choice. Worked example generated *from the shipped tool*, so the YAML printed in the guide is byte-for-byte what the converter returns)
- ✅ "How to Format XML" → XML Formatter — shipped 27 Aug 2026 alongside the tool itself (targets the "format xml" / "xml formatter" / "xml validator online" tail, part of the FreeFormatter keyword pool that is actively losing its incumbent. The guide is built around the one claim every competitor makes and few honour — that formatting does not change your document — and shows where it breaks. Whitespace between two elements is only *ignorable* when a DTD or schema declares the element to hold elements alone; with no schema, which is the normal case when someone pastes into a formatter, nothing can know that, so the safe rule is narrower: whitespace between elements in an element that holds no real text may be rearranged, and character data is never touched. The consequence is the section that earns the page — **mixed content**. `<p>Hello <b>world</b>!</p>` indented child-per-line becomes a `p` holding a newline, two spaces, "Hello", a newline, the `b`, a newline and "!"; the text was `Hello ` and is now a line break plus indentation, which renders wrong in a browser and fails a signature check, and is the commonest way an XML beautifier corrupts a file. Then the boundary case handled as a switch rather than an assumption: trimming the whitespace around a text value spread over three lines is what everyone wants *and* is strictly an edit, so it is a checkbox (default on) that `xml:space="preserve"` overrides, with the off position there for signed and canonicalised documents. Also the well-formedness list — mismatched and unclosed tags, the bare `&` that is the single commonest XML parse error, `<` in an attribute value, unquoted or duplicated attributes, text outside the root, an unterminated comment or CDATA, and a declaration that is not the first thing in the file — followed by the distinction people conflate constantly: **well-formed is not valid**, which needs the DTD or XSD only the reader has. Worked example generated *from the shipped tool*, so the XML printed in the guide is byte-for-byte what the formatter returns, and its "7 elements, four levels deep" line matches the stat the page shows)
- ✅ "How to Escape a String" → String Escaper — shipped 30 Aug 2026 alongside the tool itself (targets the "escape json string" / "html escape" / "xml escape" / "csv escape" / "escape regex" tail — the rest of the FreeFormatter keyword pool now that XML Formatter has taken the formatter half. Built on one claim, made by the output rather than asserted: **there is no such thing as an escaped string**. One line of ordinary marketing copy — `Tom & Jerry's "Best of", 50% off` — is run through all eight target formats side by side, and every line printed in the guide is the shipped module's actual output rather than hand-typed, so the page cannot drift from the tool. The same apostrophe is untouched in JSON, `&#39;` in HTML, `&apos;` in XML, doubled in SQL and a four-character close-escape-reopen dance on a command line; HTML and XML disagree on it because `&apos;` was an XML entity HTML did not define until HTML5. Then the **&-ordering bug**, which is the cheapest thing a reader can take away: the obvious three-line replace chain turns `<b>` into `&amp;lt;b&amp;gt;`, because the first replace introduces ampersands the last one escapes again — and it survives in so much code precisely because the output is still valid HTML, just wrong, so nothing crashes until a customer sees it. The section that earns the page is **the characters that cannot be escaped at all**: XML 1.0 permits exactly tab, line feed and carriage return, and `&#1;` is a parse error rather than an escape, so a document carrying a stray control character out of a legacy database cannot be made valid without removing something — a tool that just swaps the five entities hands it back silently. Plus the whitespace trap nobody documents (a parser normalises a literal CR to LF, and inside an attribute value turns tab and newline into spaces, so those survive only as `&#13;`/`&#9;`/`&#10;`). Closes on two honesty sections: escaping is **not** a security control — it protects only a value already inside quotes, so it does nothing for a numeric column, a table name or an ORDER BY direction, and it has to match the dialect's NO_BACKSLASH_ESCAPES setting, which is why a parameterised query wins — and why a regex and a shell command cannot be unescaped at all, since `\\d`, `\\b` and `\\w` are character classes rather than escaped letters. FAQs on escaping vs encoding, whether a template engine or ORM makes it unnecessary, and in-browser privacy)

- "SQL formatting and why it matters" → SQL Formatter
- ✅ "How to Unlock a PDF: Owner Restrictions vs Open Passwords" → Unlock PDF — shipped 26 Jul (the two PDF password types and why only one can be lifted: the **user/open password** derives the encryption key so the page bytes are genuinely encrypted — AES-256 in modern files, unbypassable — vs the **owner/permissions password**, where the file is encrypted with an *empty* user password so every reader already knows the key, and the no-print/no-copy/no-edit restrictions are just flags in the encryption dictionary that compliant readers honour voluntarily + the two-second test for telling them apart, prompt-before-you-see-anything vs opens-fine-but-greyed-out + why removal is lossless: decrypt with the empty password, re-save without the encryption dictionary, so the page objects, fonts, form fields and selectable text are untouched + worked example on a 6-page bank statement, identical output but Ctrl+P and text selection restored + what the tool refuses to do, and why services promising to crack strong open passwords are guessing or misleading + forgotten-password routes: password manager, the predictable DDMMYYYY/account-digits value in the covering email, ask the issuer to re-issue + entitlement note that removing the flag doesn't remove the licence or copyright behind it + in-browser privacy, which matters here because restricted PDFs skew to statements/payslips/medical letters + onward links to Split/Merge/Compress/PDF to Word, and OCR PDF when the real problem is a scan with no text layer).
- ✅ "How to Rotate a PDF and Save It Permanently" → Rotate PDF — shipped 28 Jul (the view-rotation vs saved-rotation split that explains why a page "springs back": Chrome's viewer and quick-look previews only turn the page on screen and write nothing to the file, so the change dies with the tab and recipients never see it + what rotation actually changes — one per-page value, a multiple of 90, meaning "turn this page clockwise by N when displaying or printing" — so nothing is moved, redrawn or re-encoded and rotation is genuinely lossless: no re-compression softening a scan, text stays selectable text, links and form fields survive + the MediaBox aside, a portrait page rotated 90 is still described internally as portrait and merely displayed/printed landscape + which angle: 90 puts the top edge on the right, 180 fixes upside-down feeds, and 270 clockwise *is* 90 anticlockwise since the tool offers clockwise only + worked example on the additive `(current + angle) % 360` behaviour — a 12-page scan already at 90 returns to 0 by choosing 270, because 90 + 270 = 360 wraps to 0, verified against the component's own arithmetic + the honest all-pages limitation: a mixed document needs split → rotate → merge, since rotating everything by 270 straightens page 5 but knocks upright page 1 out of true + quality/size unchanged + the password case, an open-password file can't be read so can't be rotated, while owner restrictions clear via Unlock PDF first + onward links to Organize / Remove Pages / Remove Blank Pages / Add Page Numbers / OCR, noting OCR belongs *after* rotating so the recognised text is the right way up).
- ✅ "How to Add Page Numbers to a PDF" → Add Page Numbers — shipped 2 Aug (why the numbers aren't already there: Word/Docs keep them in a *running* header-footer element that the PDF export throws away, flattening whatever it showed into fixed-coordinate page text, so there is no setting inside the file to switch on and numbering means drawing new text on every page + where to put it across the six positions, tied to what happens to the document afterwards — bottom centre for screen/single-sided because it's symmetrical however the pages are bound, bottom right as the double-sided convention since the outer edge of a recto is where a thumb lands, top right for binder/clipped documents, and avoid the edge a running header or footer already occupies because the number is drawn *on top of* it, not around it + which of the four formats: bare number for a long read-through, "Page 1" only when stray invoice/clause numbers compete, and the two "of" formats for anything printed and handed over since "1 of 10" proves no page is missing — the reason it's standard on contracts, tenders and page-limited submissions + the two settings that trip people up, both worked through against the component's own arithmetic: Skip the first page drops the cover from the *count* as well as the numbering, so a 10-page report set to "Page 1 of 10" yields a bare cover then "Page 1 of 9" … "Page 9 of 9"; and Start numbering at makes the denominator describe the whole document, so a 10-page appendix following 4 pages of front matter starting at 5 reads "5 of 14" … "14 of 14" + points geometry verified against pdf-lib's Helvetica metrics: 72pt to the inch so the default 28pt margin ≈ 9.9mm, measured edge-to-*baseline*, landing inside the 1-inch margin Word leaves rather than on the last line, while top positions reserve the full line height and sit margin+size = 39pt down; width only bites on right-hand positions and barely — "Page 1 of 10" is ~62pt and "Page 247 of 312" ~80pt against a 595pt A4 width — but keep the margin ≥20pt for print + near-zero file cost since Helvetica is a standard-14 font needing no embedding and nothing is re-rendered, so scans stay sharp, text stays selectable and links/bookmarks/form fields survive + troubleshooting the open-password case, numbers landing on existing content, and mixed page sizes each measured against their own edge + onward links to Merge / Organize / Remove Pages *before* numbering, since numbers otherwise describe an order that no longer exists, and Watermark / Compress after).
- "How to watermark a PDF" → the remaining PDF tools (Organize, Remove Pages, Remove Blank Pages)
- Converters (category now well covered — only 2 tools still lack a guide): ✅ JSON to CSV guide — "How to Convert JSON to CSV (Commas, Nesting and Delimiters Explained)" (which JSON shapes convert: array-of-objects/array-of-arrays + union-of-keys headers + RFC 4180 comma/quote/line-break escaping + nested objects → JSON-in-a-cell + comma/semicolon/tab/pipe delimiters + European decimal-comma note + worked example on Ada/Grace/Alan producing quoted "New York, NY" and an empty missing-field cell) shipped 12 Jul. ✅ Color Converter guide shipped 15 Jul (see above). ✅ CSV to JSON guide — "How to Convert CSV to JSON (Headers, Delimiters and Type Inference Explained)" (first-row-as-keys → array of objects vs header-off → array of arrays + strict type inference: true/false → booleans, bare numbers → numbers, and the leading-zero trap where 0074 stays "0074" but 1200 becomes a number so ID columns go mixed-type → turn typing off + comma/semicolon/tab/pipe delimiters with European decimal-comma note + RFC 4180 quoted commas/newlines/escaped-quotes + worked example on sku,name,price,in_stock producing "Widget, deluxe" as one value, an empty missing-price cell, and the mixed 0074/1200 sku column — verified against the tool's own parser/coercion logic) shipped 22 Jul. ✅ YAML to JSON guide shipped 23 Jul. ✅ Number to Words guide shipped 1 Aug. ✅ CGPA to Percentage guide shipped 6 Aug (see above). ✅ Rupees in Words guide shipped 7 Aug alongside the tool (see above). Remaining convert tools without a guide: Hex to RGB, RGB to Hex.
- Text & writing (new guide category): ✅ Word Counter guide — "Word Count, Character Count and Reading Time Explained" (whitespace word rule + chars with/without spaces + reading-time estimate at 200–250 wpm + platform limits: X 280 / SMS 160 / meta 150–160 / Instagram 2,200 + words-per-page 500 single / 250 double) shipped 10 Jul. First guide under the new **Text & writing** hub category. ✅ Case Converter guide — "Text Case Formats Explained: UPPERCASE, Title Case, camelCase and snake_case" (exact behaviour of all six formats + title-case rules AP/Chicago don't match + why chaining conversions loses the original + camelCase vs snake_case for code + worked example on "User First Name" + which case to use where) shipped 11 Jul. ✅ Lorem Ipsum guide — "What Is Lorem Ipsum? Meaning, History and When to Use It" (what greeking is + Cicero de Finibus 45 BC origin + 'lorem' = truncated 'dolorem' + Letraset/PageMaker revival + McClintock tracing 'consectetur' + why designers use neutral filler + paragraphs/sentences/words + themed-ipsum caveat + never ship it: SEO/screen-reader/content-first) shipped 13 Jul. Remaining text tools without a guide: Remove Line Breaks, Remove Duplicate Lines, Find & Replace, Text Reverser.
- High-search-volume finance calculators still lacking a guide: **all finance calculators now have guides.** ✅ Inflation guide (future-cost + purchasing-power formulas + real-return vs headline-return + Rule of 70 halving) shipped 9 Jul. ✅ Simple Interest guide (SI = P × R × T / 100 term-by-term + worked example + rearrangements + simple vs compound) shipped 8 Jul. ✅ PPF guide shipped 27 Jun. ✅ Income Tax guide (old vs new regime) shipped 28 Jun. ✅ HRA exemption guide shipped 29 Jun. ✅ In-Hand Salary guide (CTC → take-home) shipped 30 Jun. ✅ Gratuity guide (15/26 formula) shipped 1 Jul. ✅ NPS guide (corpus + 40% annuity + pension) shipped 2 Jul. ✅ RD guide (quarterly-compounding maturity formula) shipped 3 Jul. ✅ Lumpsum guide (compound-growth formula + Rule of 72 + lumpsum vs SIP) shipped 4 Jul. ✅ SWP guide (month-by-month withdrawal loop + closed-form check + depletion + safe-withdrawal rule) shipped 5 Jul. ✅ CAGR guide (geometric-root formula + why it beats a simple average + CAGR vs total return + Rule of 72) shipped 6 Jul. ✅ ROI guide (net-gain formula + total vs annualized ROI + negative ROI + what ROI leaves out) shipped 7 Jul.

Let GSC Performance data pick the order — write guides for whichever live tools are already drawing impressions.

**Long-tail strategy (from 5 Jul audit):** for finance, don't chase head terms ("SIP calculator") that Groww/ClearTax/ET Money own — write for question-shaped queries a new domain can win now ("is gratuity taxable after 5 years", "PPF vs FD which is better", "how much SIP for 1 crore in 15 years"). Once every tool has its explainer guide, the next content layer is **comparison/scenario guides** (PPF vs FD, lumpsum vs SIP, old vs new regime already exists) that can link to multiple calculators.

## Phase 4 — On-page SEO polish (one focused pass) ⏳ NOT STARTED

The architecture already does most of this well (per-tool titles, canonical URLs, OpenGraph, SoftwareApplication + BreadcrumbList + FAQPage JSON-LD). Tighten:
- **Unique, keyword-led meta descriptions** per tool (a few currently read generically — make each one match how people actually search). Worth a dedicated pass now that there are 56 of them.
- ~~**OG image.**~~ ✅ DONE (9 Jun 2026). Branded 1200×630 `public/og.png` wired into root layout + `lib/seo.js`; `twitter:card` upgraded to `summary_large_image`. Verified live via opengraph.xyz. Source: `public/og.svg` (edit + re-render with sharp/ImageMagick if text changes). Future option: per-page dynamic OG images (Next `ImageResponse`) — nice-to-have, not needed.
- **Homepage copy.** Make the H1 and intro target "free online tools" + the categories, so the homepage itself ranks.
- **Related-tools internal linking** is already present (good) — keep giving every new tool 2+ FAQs. **Expectation check (5 Jul audit):** Google stopped showing FAQ rich results for most non-government/health sites in 2023, so the FAQPage schema won't produce visible SERP snippets — keep it (harmless, and the FAQ *content* still ranks for question queries), but don't invest more expecting rich results.

## Phase 5 — Off-site / distribution (now unblocked — 20+ tools live) ⏳ NOT STARTED — **now the biggest gap (5 Jul audit)**

The "do after 20+ tools live" gate is cleared (56 live). With technical SEO done and content compounding, **zero referring domains is what's left holding the site in the cold-start sandbox.** Concrete channels, cheapest first:
- **Tool directories & aggregators:** AlternativeTo, ToolFinder-style lists, free-tool roundups. Low effort, legitimate links.
- **Product Hunt / Hacker News "Show HN"** for the suite — the PDF suite (client-side, no uploads) and Passport Photo Maker are the strongest angles; even modest traction earns real links.
- **Helpful forum answers:** r/IndiaInvestments, r/personalfinanceindia and similar — link a calculator only where it genuinely answers the question, sparingly and honestly.
- **Dev-facing content:** write dev.to/Hashnode posts about the underlying concepts (JWT internals, Base64, hashing) that naturally reference the tools.
- A few quality backlinks from dev/finance communities move the needle more than volume.

---

## Suggested rhythm

| Cadence | Action |
|---|---|
| Each session | Write 1–2 guides (Phase 3) or polish meta descriptions (Phase 4); easy-tool backlog is now cleared |
| Weekly | 10-min GSC ritual: sort queries by impressions → (a) tools drawing impressions with no guide = next guide to write; (b) guides ranking positions 8–20 = expand/refresh them (small gains there reach page 1); bump `updated` on real revisions |
| Monthly | Write 2–3 guides for top-trafficked tools; refresh any stale meta descriptions; one Phase 5 distribution action (directory submission, forum answer, or a Show HN when ready) |
| Quarterly | PageSpeed audit (next one should confirm the PDF deps are clean); review tool meta descriptions by GSC data |
| Yearly | Refresh the tax/salary calculators (Income Tax, In-Hand Salary) when FY slabs change; update the "last updated" notes |

**The one habit that matters most:** ship pages consistently and watch Search Console. Traffic on a tools site compounds — 77 tools + 67 guides today, each one earning its own trickle of search traffic that adds up. The next lever is **content (guides) and SEO polish**, not more easy tools.
